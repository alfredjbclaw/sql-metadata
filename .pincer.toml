# pincer per-repo override for the sql-metadata fork.
# Bare Ubuntu VM: bootstrap pip, install the package + pytest, run the suite.
test_command = """sudo apt-get update -qq && sudo apt-get install -y -qq python3-pip && python3 -m pip install --break-system-packages -q -e . pytest && python3 -m pytest test/ -q"""
